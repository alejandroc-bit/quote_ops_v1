services:
  quoteops-agent:
    image: quote-ops-agent:local
    build:
      context: ../..
      dockerfile: deploy/appliance/Dockerfile
      args:
        QUOTEOPS_SERVICE: agent
    environment:
      PORT: 8081

  quoteops-api:
    image: quote-ops-api:local
    build:
      context: ../..
      dockerfile: deploy/appliance/Dockerfile
      args:
        QUOTEOPS_SERVICE: api

  quoteops-web:
    image: quote-ops-web:local
    build:
      context: ../..
      dockerfile: deploy/appliance/Dockerfile.web
